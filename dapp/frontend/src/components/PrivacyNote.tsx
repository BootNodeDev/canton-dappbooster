// Shown when the acting party sees nothing, because the ledger only discloses what you are a
// stakeholder of. The privacy is real, not simulated.
export const PrivacyNote = (): React.JSX.Element => (
  <p className="mt-4 text-center font-mono text-[0.7rem] text-fg-soft">
    privacy holds — you see only what you're party to
  </p>
)
