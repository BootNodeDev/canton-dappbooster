import { type IconProps, Svg } from '@/icons/Svg'

export const CaretDownIcon = (p: IconProps): React.JSX.Element => (
  <Svg fill="currentColor" stroke="none" {...p}>
    <path d="M4 8h16l-8 10z" />
  </Svg>
)
