# Splice Scan: the dApp links party ids here, and reads the AmuletRules and the open mining round
# every Amulet-moving write discloses off its /api/scan. Point it elsewhere and the writes fail.
VITE_EXPLORER_URL=http://scan.localhost:4000
