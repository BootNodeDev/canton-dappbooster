set -e

echo "TypeScript checks: dapp/frontend"
pnpm -C dapp/frontend exec tsc -b --noEmit --pretty

echo "TypeScript checks: canton-barebones/wallet-service"
pnpm -C canton-barebones/wallet-service exec tsc --noEmit --pretty

echo "TypeScript checks: canton-connect-kit"
pnpm -C canton-connect-kit exec tsc -b --noEmit --pretty

echo "All TypeScript checks passed. Proceeding with push."
